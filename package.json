{
  "name": "ball-countries",
  "version": "0.1.0",
  "description": "کشورهای توپی: فرمانروایی جهان — بازی استراتژیک موبایلی",
  "private": true,
  "scripts": {
    "serve": "npx http-server www -p 8080 -c-1",
    "android:add": "npx cap add android",
    "android:sync": "npx cap sync android",
    "android:open": "npx cap open android",
    "apk:debug": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
